version: 0.2

phases:
  install:
    commands:
      - echo "installing"
  build:
    commands:
      - echo "building"
  post_build:
    commands:
      - echo "running tests"
      - bash metrics.sh > logs.json
      - cat logs.json

