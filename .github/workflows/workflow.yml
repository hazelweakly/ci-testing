name: Test things
on:
  push:
    branches:
      - master
      - "new-*"
  pull_request:

jobs:
  wat:
    runs-on: ubuntu-latest
    steps:
      - if: "startsWith(github.ref, 'refs/tags/v')"
        run: echo "sup"
